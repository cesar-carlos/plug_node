---
"n8n-nodes-plug-database": patch
---

Extract guided SQL command builders into shared modules, add unit tests for hub SQL options and batch/bulk/cancel payloads, and extend live E2E suites for `sql.executeBatch`, hub options, and gated `sql.cancel`.
