---
"n8n-nodes-plug-database": minor
---

Add Auto Performance Hints (default on) for Socket SQL streaming and read-only batch parallelism, validate bulk insert against hub row/size limits before dispatch, and align relay stream pull window fallback to 256.
