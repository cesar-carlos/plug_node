---
"n8n-nodes-plug-database-advanced": minor
---

Add and harden advanced Socket event UX: publish `client:custom.*` events over REST or Socket, support binary attachments, trigger on custom events or `client:agent.profile.updated`, add backpressure/signature controls, and document the public and internal Custom Socket Event flows.
