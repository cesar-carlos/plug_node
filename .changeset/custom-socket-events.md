---
"n8n-nodes-plug-database-advanced": minor
---

Add and harden advanced Socket event UX: publish `client:custom.*` events over REST or Socket, support binary attachments with server-aligned local limits, trigger on custom events or `client:agent.profile.updated`, add backpressure/signature/deduplication/reconnect controls, and document the public and internal Custom Socket Event flows.
