---
"n8n-nodes-plug-database": minor
---

Automatic AI Hub → MCP Server wiring: Hub emits a `wiring` block; MCP Server inherits forbidden names and max tool calls from input/`aiHubNodeName` when params stay at defaults. CI early format job, version-PR Check Runs, Linux baseline calibration artifact, staging e2e hub, pack-size warnings, and expanded socket unit coverage.
