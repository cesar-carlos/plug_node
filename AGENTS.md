# AGENTS.md

This repository uses `D:\Developer\plug_database\plug_node\.cursor\rules` as the source of truth for project-specific coding and architecture guidance.

## What to read

- Read the rule files in `D:\Developer\plug_database\plug_node\.cursor\rules` before making structural or architectural changes.
- Treat `AGENTS.md` as an index only. Do not duplicate the detailed rules here.

## Rule focus

- The rules are tailored for a workspace that builds n8n node packages for Plug.
- They cover architecture, package structure, stack choices, security, testing, performance, governance, and TypeScript conventions.

## Implementation note

- If a rule still looks inherited from an older project, update the owning file in `.cursor/rules` instead of adding exceptions here.
